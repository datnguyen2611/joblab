import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme: any) => {
  return {
    contentConvertPage: {
      position: "relative",
      background: "#F6F6F9",
      paddingBottom: 55,
      minHeight: "100vh",
    },
  };
});

export default useStyles;
